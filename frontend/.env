# 프론트에서 사용할 백엔드 API 기본 주소
# - 로컬 개발은 vite proxy(/api)로도 동작하므로 보통 필요 없음
# - 백엔드를 다른 호스트/포트에서 띄울 때만 설정
# 수정된 .env (루트 및 프론트 동일하게 적용)
VITE_API_URL=/api
VITE_AI_URL=/ai
IMG_BASE_URL=http://[NCP_공인_IP]:5000/uploads